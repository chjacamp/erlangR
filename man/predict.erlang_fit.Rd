% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{predict.erlang_fit}
\alias{predict.erlang_fit}
\title{Predict abandonment as a function of agents on duty}
\usage{
\method{predict}{erlang_fit}(object, n_agents, arrivals_per_hour = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{An \code{\link[=erlang_fit]{erlang_fit()}} object.}

\item{n_agents}{Integer vector of staffing levels to evaluate.}

\item{arrivals_per_hour}{Arrival rate to predict at. Defaults to the
average fitted rate in the training data.}

\item{level}{Level for the patience-uncertainty band.}

\item{...}{Unused.}
}
\value{
A data frame of class \code{"erlang_pred"} with columns \code{n_agents},
\code{p_abandon}, \code{lwr}, \code{upr}, \code{abandoned_per_hour}, \code{occupancy}.
}
\description{
The decision curve: \code{P(abandon | n_agents)} at the fitted service and
patience rates, for a stated arrival rate. The uncertainty band
propagates the patience confidence interval (arrival and service rates
are treated as plug-ins).
}
\examples{
fit <- erlang_fit(callcenter, rate = ~ dow + tod)
predict(fit, n_agents = 8:24, arrivals_per_hour = 120)
}
