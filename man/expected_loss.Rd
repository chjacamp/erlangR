% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/economics.R
\name{expected_loss}
\alias{expected_loss}
\title{Expected loss of a staffing level}
\usage{
expected_loss(pred, value_per_call, cost_per_agent_hour)
}
\arguments{
\item{pred}{An \code{"erlang_pred"} object from \code{\link[=predict.erlang_fit]{predict.erlang_fit()}}.}

\item{value_per_call}{Revenue (or value) lost when one call abandons.}

\item{cost_per_agent_hour}{Fully loaded hourly cost of one agent on duty.}
}
\value{
A data frame of class \code{"erlang_loss"} with columns \code{n_agents},
\code{p_abandon}, \code{lost_per_hour}, \code{staff_per_hour}, \code{total_per_hour}. The
cost-minimising staffing level is in \code{attr(, "optimum")}.
}
\description{
Converts the predicted abandonment curve into money: lost revenue from
abandoned calls against the payroll cost of agents, per hour. This is the
decision framing the package is built around -- pick \code{n} to minimise
expected total cost, rather than to hit a service-level target.
}
\details{
The result is a model prediction, not a verdict: the honest next step is
an experiment (e.g. alternate staffing levels week by week, as the
\link{callcenter} example data does) to check the predicted differences where
it matters.
}
\examples{
fit <- erlang_fit(callcenter)
pred <- predict(fit, n_agents = 6:24, arrivals_per_hour = 120)
loss <- expected_loss(pred, value_per_call = 30, cost_per_agent_hour = 26)
loss
plot(loss)
}
