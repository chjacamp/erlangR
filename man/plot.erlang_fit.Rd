% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.erlang_fit}
\alias{plot.erlang_fit}
\title{Plot method for fitted Erlang-A models}
\usage{
\method{plot}{erlang_fit}(x, type = c("calibration", "series"), days = 7, ...)
}
\arguments{
\item{x}{An \code{\link[=erlang_fit]{erlang_fit()}} object.}

\item{type}{\code{"calibration"} or \code{"series"}.}

\item{days}{Number of days to show when \code{type = "series"} (requires a
\code{ts} timestamp column in the fitting data).}

\item{...}{Passed to \code{\link[=plot]{plot()}}.}
}
\value{
Invisibly, \code{x}.
}
\description{
\code{type = "calibration"} plots observed per-interval abandonment rates
against the fitted \code{P(abandon)} (point area proportional to arrivals, so
the eye weights intervals the way the likelihood does). A well-specified
model scatters around the 45-degree line.
\code{type = "series"} overlays observed and fitted abandonment for the first
\code{days} days of data -- the day-to-day view a workforce planner recognises.
}
