% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erlang-bc.R
\name{erlang_b}
\alias{erlang_b}
\alias{erlang_c}
\title{Erlang B and Erlang C}
\usage{
erlang_b(n_agents, offered)

erlang_c(n_agents, offered)
}
\arguments{
\item{n_agents}{Number of agents (non-negative integer, vectorised).}

\item{offered}{Offered load in erlangs: \code{lambda / mu}.}
}
\value{
Numeric vector of probabilities.
}
\description{
The classical loss and delay formulas, included as limiting cases of the
Erlang-A model and for reference. \code{erlang_b()} is the blocking
probability of M/M/n/n (arrivals finding all agents busy are lost);
\code{erlang_c()} is the probability of waiting in M/M/n (infinite patience).
}
\details{
\code{erlang_c()} is the \code{theta -> 0} limit of \code{\link[=p_wait]{p_wait()}}; Erlang C has no
abandonment and is therefore only defined as a stationary system when
\code{offered < n_agents}. For \code{offered >= n_agents} the queue diverges and
\code{erlang_c()} returns 1.

Both use the standard numerically stable recursion (no factorials), so
they are exact to machine precision for thousands of agents.
}
\examples{
erlang_b(2, 1)   # exactly 1/5
erlang_c(2, 1)   # exactly 1/3

# Erlang C as the patient limit of Erlang A:
erlang_c(10, 8)
p_wait(10, lambda = 8, mu = 1, theta = 1e-8)
}
