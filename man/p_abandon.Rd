% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erlang-a.R
\name{p_abandon}
\alias{p_abandon}
\alias{p_wait}
\alias{mean_wait}
\alias{occupancy}
\title{Erlang-A (M/M/n+M) performance measures}
\usage{
p_abandon(n_agents, lambda, mu, theta)

p_wait(n_agents, lambda, mu, theta)

mean_wait(n_agents, lambda, mu, theta)

occupancy(n_agents, lambda, mu, theta)
}
\arguments{
\item{n_agents}{Number of agents on duty (non-negative integer,
vectorised).}

\item{lambda}{Arrival rate (calls per unit time).}

\item{mu}{Service rate per agent (\verb{1 / mean handle time}).}

\item{theta}{Abandonment rate while waiting (\verb{1 / mean patience}).}
}
\value{
A numeric vector:
\describe{
\item{\code{p_abandon()}}{probability an arriving call eventually abandons.}
\item{\code{p_wait()}}{probability an arriving call is not served
immediately (all agents busy on arrival).}
\item{\code{mean_wait()}}{mean time spent waiting in queue, across all
arrivals (served and abandoning), in the unit implied by the rates.}
\item{\code{occupancy()}}{fraction of paid agent time spent handling calls,
\verb{E[busy agents] / n_agents} (an output of the model, not an input
constraint).}
}
}
\description{
Exact stationary performance measures of the Erlang-A queue: Poisson
arrivals at rate \code{lambda}, \code{n_agents} parallel agents with exponential
service at rate \code{mu} each (mean handle time \code{1/mu}), and exponentially
distributed caller patience with mean \code{1/theta} -- a caller still waiting
when their patience runs out abandons.
}
\details{
The model has exactly three parameters (\code{lambda}, \code{mu}, \code{theta}); the
number of agents \code{n_agents} is the decision variable. All rates share one
arbitrary time unit -- per second, per minute, per hour -- as long as you
are consistent. \code{p_abandon()} is the headline quantity:
\code{P(abandon | n_agents)}.

Measures are computed from the stationary birth--death distribution (in
log space, so thousands of agents are fine) rather than from tabulated
approximations, and satisfy the exact conservation laws of the chain,
e.g. \verb{lambda * p_abandon = theta * E[queue length]} and
\code{lambda * (1 - p_abandon) = mu * n_agents * occupancy}.

Unlike Erlang C, the system is stable for every \code{lambda} (abandonment
drains any overload), so these functions are defined -- and meaningful --
even when \code{lambda >= n_agents * mu}.
}
\examples{
# 120 calls/hour, 4-minute mean handle, 2-minute mean patience (per-minute units)
p_abandon(n_agents = 6:12, lambda = 2, mu = 1/4, theta = 1/2)

# the decision curve is what matters:
plot(4:16, p_abandon(4:16, 2, 1/4, 1/2), type = "b",
     xlab = "agents", ylab = "P(abandon)")
}
\references{
Palm, C. (1946). Research on telephone traffic carried by full
availability groups. \emph{Tele}, 1, 107.

Mandelbaum, A. and Zeltyn, S. (2007). Service engineering in action: the
Palm/Erlang-A queue. In \emph{Advances in Services Innovations}, 17--45.
}
\seealso{
\code{\link[=erlang_a]{erlang_a()}} for all measures at once, \code{\link[=erlang_fit]{erlang_fit()}} to
estimate the parameters from data, \code{\link[=simulate_erlang_a]{simulate_erlang_a()}} for a
validating simulator, \code{\link[=erlang_c]{erlang_c()}} for the no-abandonment limit.
}
