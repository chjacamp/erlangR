% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{agents_for_abandon}
\alias{agents_for_abandon}
\title{Smallest staffing level meeting an abandonment target}
\usage{
agents_for_abandon(target, lambda, mu, theta, max_agents = 10000)
}
\arguments{
\item{target}{Maximum acceptable \code{P(abandon)}.}

\item{lambda}{Arrival rate (calls per unit time).}

\item{mu}{Service rate per agent (\verb{1 / mean handle time}).}

\item{theta}{Abandonment rate while waiting (\verb{1 / mean patience}).}

\item{max_agents}{Search cap.}
}
\value{
Integer staffing level.
}
\description{
Inverse of \code{\link[=p_abandon]{p_abandon()}} in the decision variable: the fewest agents for
which predicted abandonment does not exceed \code{target}. Provided as a
convenience; the package's preferred framing is \code{\link[=expected_loss]{expected_loss()}}, which
makes the cost of the target explicit instead of treating it as a given.
}
\examples{
agents_for_abandon(0.02, lambda = 2, mu = 1/4, theta = 1/2)
}
