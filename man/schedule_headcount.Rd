% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{schedule_headcount}
\alias{schedule_headcount}
\title{Translate agents on duty into scheduled headcount}
\usage{
schedule_headcount(n_agents, shrinkage = 0.3, granularity = 0.5)
}
\arguments{
\item{n_agents}{Agents required on duty (vectorised).}

\item{shrinkage}{Fraction of scheduled time an agent is not available to
handle contacts (0 to <1).}

\item{granularity}{Rounding step for the schedule (default half an
agent, i.e. one part-time slot); result is rounded up.}
}
\value{
Scheduled headcount, \code{n_agents / (1 - shrinkage)} rounded up to
\code{granularity}.
}
\description{
The model's \code{n_agents} is bodies actually answering during the interval.
Rosters lose time to breaks, meetings, sickness and holidays
("shrinkage"), so the scheduled headcount must be larger. This helper
performs that translation -- deliberately outside the model: shrinkage is
scheduling policy, not a parameter of the queue.
}
\examples{
schedule_headcount(12, shrinkage = 0.3)
}
