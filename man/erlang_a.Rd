% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erlang-a.R
\name{erlang_a}
\alias{erlang_a}
\title{All Erlang-A measures for one configuration}
\usage{
erlang_a(n_agents, lambda, mu, theta)
}
\arguments{
\item{n_agents}{Number of agents on duty (non-negative integer,
vectorised).}

\item{lambda}{Arrival rate (calls per unit time).}

\item{mu}{Service rate per agent (\verb{1 / mean handle time}).}

\item{theta}{Abandonment rate while waiting (\verb{1 / mean patience}).}
}
\value{
An object of class \code{"erlang_a"}: a list with the four measures,
the offered load \code{lambda / mu}, and the parameters.
}
\description{
Convenience wrapper computing every measure in \code{\link[=p_abandon]{p_abandon()}} at a single
configuration, returned as a small object with a readable print method.
}
\examples{
erlang_a(n_agents = 10, lambda = 2, mu = 1/4, theta = 1/2)
}
