% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_erlang_a}
\alias{simulate_erlang_a}
\title{Simulate an Erlang-A queue call by call}
\usage{
simulate_erlang_a(lambda, mu, theta, n_agents, horizon, seed = NULL)
}
\arguments{
\item{lambda}{Arrival rate (calls per unit time).}

\item{mu}{Service rate per agent (\verb{1 / mean handle time}).}

\item{theta}{Abandonment rate while waiting (\verb{1 / mean patience}).}

\item{n_agents}{Number of agents on duty (non-negative integer,
vectorised).}

\item{horizon}{Length of simulated time.}

\item{seed}{Optional integer; if supplied, sets the RNG seed for
reproducibility.}
}
\value{
A data frame with one row per call: \code{arrival} (time), \code{wait}
(time spent in queue -- offered wait if served, patience if abandoned),
\code{served} (logical), and \code{handle} (service duration, \code{NA} for abandoned
calls). A list of realised summary statistics is attached as
\code{attr(, "summary")}.
}
\description{
Discrete-event simulation of the M/M/n+M queue under first-come
first-served routing: each call draws an exponential patience and an
exponential service requirement; a call abandons if no agent frees up
before its patience runs out. Useful for validating the analytic
formulas, for power analysis when designing staffing experiments, and as
the generator behind the \link{callcenter} example data.
}
\details{
All rates share one time unit, as everywhere in the package. The
simulator is exact for the model (no time discretisation).
}
\examples{
sim <- simulate_erlang_a(lambda = 2, mu = 1/4, theta = 1/2,
                         n_agents = 9, horizon = 5000, seed = 1)
attr(sim, "summary")$p_abandon
p_abandon(9, 2, 1/4, 1/2)   # analytic counterpart
}
